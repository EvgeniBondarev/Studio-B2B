# 📋 ТЗ: Прототип интерфейса Multi-Tenant SaaS

> Цель: создать кликабельный прототип UI со всеми экранами и заглушками для функций. Без реальной бизнес-логики и БД — только интерфейс для проверки UX и навигации.

---

## Стек прототипа

- **ASP.NET Core 8** + **Razor Pages** (или Blazor Server)
- **MudBlazor** (UI-компоненты)
- Данные: **захардкоженные моки** (статические списки в коде)
- Без БД, без EF Core, без Redis

---

## 1. Авторизация и мультитенант

### 1.1 Страница входа `/login`
- Поля: Email, Пароль
- Кнопка "Войти" → редирект на `/orders`
- Ссылка "Регистрация нового клиента"
- Заглушка: любой логин/пароль проходит, tenant берётся из субдомена (или хардкод)

### 1.2 Регистрация клиента `/register`
- Поля: Название компании, Email, Пароль, Поддомен (slug)
- Кнопка "Создать аккаунт" → показать toast "Аккаунт создан" → редирект на `/login`
- Заглушка: ничего не сохраняется

---

## 2. Основной Layout

### 2.1 Боковое меню (Sidebar)
- Логотип + название компании тенанта
- Пункты:
  - 📦 **Заказы** `/orders`
  - 🔄 **Транзакции** `/transactions`
  - 🏪 **Клиенты** `/clients`
  - 📊 **Поставщики** `/suppliers`
  - 🏭 **Склады** `/warehouses`
  - ⚙️ **Настройки** `/settings`
  - 🔗 **Интеграция 1С** `/settings/1c`
  - 📋 **Конструктор транзакций** `/settings/transaction-definitions`

### 2.2 Верхняя панель (AppBar)
- Имя пользователя + роль
- Кнопка уведомлений (badge с числом)
- Кнопка выхода

---

## 3. Заказы

### 3.1 Список заказов `/orders`
- Таблица с колонками: №, Дата, Маркетплейс, Статус, Клиент, Сумма, Склад
- Фильтры: по статусу (чипсы), по маркетплейсу, по дате (диапазон)
- Поиск по номеру заказа
- Пагинация (25/50/100)
- Кнопка "Синхронизировать" (заглушка: toast "Синхронизация запущена")
- Мок: 20–30 заказов с разными статусами

### 3.2 Карточка заказа `/orders/{id}`
- Шапка: номер, статус (цветной badge), маркетплейс, дата
- Блок "Клиент": ИНН, название, склад
- Блок "Поставщик": название, ИНН
- Таблица товаров: Артикул, Название, Кол-во, Цена, Сумма
- Блок "История транзакций" (список проведений по этому заказу)
- **Кнопки действий** — динамические, зависят от текущего статуса:
  - Например для статуса "В работе": "Заказ поставщику", "Заказ реализатору", "Отмена"
  - Для "Заказан": "Смена поставщика", "Принять на склад"
  - Заглушка: кнопки берутся из мок-списка `TransactionDefinition` по `FromStatusId`

### 3.3 Диалог проведения транзакции (Modal)
- Заголовок: название транзакции (например "Заказ поставщику")
- **Динамическая форма** — поля генерируются из мок-`FormSchemaJson`:
  - `Type: "Select"` → выпадающий список (данные из мока)
  - `Type: "Date"` → DatePicker
  - `Type: "Text"` → текстовое поле
  - `Type: "Number"` → числовое поле
  - `Type: "Checkbox"` → чекбокс
- Поле "Комментарий" (всегда)
- Кнопка "Провести" → toast "Транзакция проведена. Документ 1С отправлен в очередь" → обновить статус в моке
- Кнопка "Отмена"

---

## 4. Транзакции (журнал проведений)

### 4.1 Список транзакций `/transactions`
- Таблица: Дата, Заказ №, Тип операции, Пользователь, Статус 1С
- Фильтры: по типу операции, по статусу 1С (Pending/Success/Error)
- Статус 1С: цветной badge (🟡 Pending, 🟢 Success, 🔴 Error)
- Клик по строке → раскрытие деталей (InputData, OneCId, ошибка)
- Кнопка "Повторить отправку в 1С" (для Error) — заглушка

---

## 5. Справочники

### 5.1 Клиенты `/clients`
- Таблица: Название, ИНН, Склад, Кол-во заказов
- Кнопка "Добавить" → диалог с полями (Название, ИНН, Склад)
- Редактирование по клику

### 5.2 Поставщики `/suppliers`
- Таблица: Название, ИНН, НДС (да/нет), Кол-во заказов
- Кнопка "Добавить"

### 5.3 Склады и маппинг `/warehouses`
- Таблица маппинга: Склад маркетплейса → Склад 1С
- Кнопка "Добавить связь"
- Кнопка "Загрузить склады из 1С" → заглушка: toast "Загружено 5 складов"

---

## 6. Настройки

### 6.1 Настройки компании `/settings`
- Название компании
- Поддомен
- Тарифный план (readonly)

### 6.2 Интеграция с 1С `/settings/1c`
- Поля: URL сервера 1С, Логин, Пароль, ИНН организации
- Кнопка "Проверить подключение" → заглушка: toast "Подключение успешно" (или "Ошибка")
- Переключатель "Включить интеграцию"
- Блок "Справочники 1С":
  - Кнопка "Загрузить склады" → показать список в таблице
  - Кнопка "Загрузить договоры" → показать список
  - Кнопка "Загрузить поставщиков" → показать список

### 6.3 Конструктор транзакций `/settings/transaction-definitions`
- Таблица: Название, Из статуса → В статус, Документ 1С, Активна
- Кнопка "Создать транзакцию" → форма:
  - Название операции
  - Начальный статус (Select)
  - Конечный статус (Select)
  - Тип документа 1С (Select: Movement / Receipt / None)
  - **Конструктор полей формы** (добавить/удалить поля):
    - Ключ, Тип (Select/Date/Text/Number), Обязательное, Подпись
  - **Маппинг полей** (ключ формы → поле заказа)
- Кнопка "Сохранить" → toast + добавить в мок-список

---

## 7. Мок-данные (заглушки)

### Статусы заказов
```csharp
new[] { "Новый", "В работе", "Заказан", "Отгружен", "Доставлен", "Возврат", "Отменён" }
```

### Маркетплейсы
```csharp
new[] { "Ozon", "Wildberries", "Yandex Market" }
```

### Типы транзакций (мок TransactionDefinition)
| Название | Из статуса | В статус | Документ 1С |
|---|---|---|---|
| Принять в работу | Новый | В работе | — |
| Заказ поставщику | В работе | Заказан | Receipt |
| Заказ реализатору | В работе | Отгружен | Movement |
| Смена поставщика | Заказан | Заказан | Movement (Reversed) |
| Принять на склад | Заказан | Доставлен | Movement |
| Возврат на склад | Доставлен | Возврат | Movement (Reversed) |
| Отмена | * | Отменён | — |

### Мок-заказы
20–30 записей с разными статусами, маркетплейсами, клиентами, товарами.

### Мок-справочники 1С
- 5 складов (`WarehouseResponse`)
- 3 договора (`TreatyResponse`)
- 4 поставщика (`SupplierResponse`)

---

## 8. Что НЕ входит в прототип

- ❌ Реальная БД и EF Core
- ❌ Реальная авторизация (Identity)
- ❌ Реальные HTTP-запросы к 1С и маркетплейсам
- ❌ Hangfire и фоновые задачи
- ❌ Redis и кэширование
- ❌ Мультитенантность (один хардкод-тенант)
- ❌ Валидация через FluentValidation

---

## 9. Критерии готовности

- [ ] Все экраны из п.1–6 открываются и отображают мок-данные
- [ ] Навигация между экранами работает
- [ ] Динамическая форма проведения генерируется из мок-`FormSchemaJson`
- [ ] Кнопки действий на карточке заказа зависят от текущего статуса
- [ ] Все кнопки действий показывают toast с результатом
- [ ] Таблицы поддерживают сортировку, фильтрацию, пагинацию
- [ ] Адаптивный layout (sidebar сворачивается на мобильных)

---

## 🔗 Связанные документы

- [Конструктор транзакций](./транзакции.md)
- [Интеграция с 1С](./интеграция%20с%201С.md)
- [Адаптеры заказов](./Адаптеры%20заказов.md)
- [UI фреймворк](./UI.md)
- [Razor Pages](./Razor.md)
