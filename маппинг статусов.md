Это классическая проблема интеграции, которую можно назвать **"Вавилонская башня статусов"**.

*   **Ozon:** `awaiting_packaging`, `awaiting_deliver`, `delivering`, `delivered`...
*   **WB:** `confirm`, `sold`, `canceled`...
*   **1C:** `Утвержден`, `КОтгрузке`, `Закрыт`...
*   **Твоя система:** `New`, `InProgress`, `Shipped`...

Если захардкодить это в коде (`if ozon_status == "awaiting_packaging" return InProgress`), то при любом изменении API маркетплейса или желании клиента настроить процесс под себя, придется переписывать код.

В SaaS архитектуре решение должно быть **динамическим** и **настраиваемым**.

---

### 1. Модель данных: Карта соответствия (Mapping Matrix)

Нам нужно хранить правила перевода. Причем нужны **системные настройки** (по умолчанию) и **пользовательские настройки** (переопределение).

**Таблица `StatusMappings`:**

```csharp
public class StatusMapping
{
    public Guid Id { get; set; }
    public Guid? TenantId { get; set; } // Если null — это глобальная настройка (системная)
    
    public MarketplaceType Source { get; set; } // Ozon, WB, Yandex
    public string ExternalStatusKey { get; set; } // Строка из API: "awaiting_packaging"
    public string ExternalStatusName { get; set; } // Читаемое имя: "Ожидает упаковки"
    
    public int InternalStatusId { get; set; } // ID статуса в ТВОЕЙ системе
    public OrderStatus InternalStatus { get; set; }
}
```

---

### 2. Сервис разрешения статусов (Status Resolver)

Этот сервис используется внутри твоих **"Кастеров"** (Мапперов) при импорте заказов.
Он работает по стратегии **Fallback**:
1.  Ищем настройку конкретного **Тенанта**.
2.  Если нет — ищем **Глобальную** настройку.
3.  Если нет — возвращаем спец. статус **"Ошибка маппинга"** (или "Неразобранное").

```csharp
public class StatusResolver
{
    private readonly TenantDbContext _db;
    private readonly IMemoryCache _cache; // Кэшируем, т.к. статусов мало, а запросов много

    public StatusResolver(TenantDbContext db, IMemoryCache cache)
    {
        _db = db;
        _cache = cache;
    }

    public async Task<int> ResolveAsync(Guid tenantId, MarketplaceType source, string externalKey)
    {
        string cacheKey = $"status_map:{tenantId}:{source}:{externalKey}";
        
        return await _cache.GetOrCreateAsync(cacheKey, async entry =>
        {
            // 1. Ищем настройку пользователя
            var userMap = await _db.StatusMappings
                .FirstOrDefaultAsync(x => x.TenantId == tenantId 
                                       && x.Source == source 
                                       && x.ExternalStatusKey == externalKey);

            if (userMap != null) return userMap.InternalStatusId;

            // 2. Ищем глобальную настройку (TenantId == null)
            // (В реальности это может быть запрос в MasterDB или закешированный справочник)
            var globalMap = await _db.StatusMappings
                .FirstOrDefaultAsync(x => x.TenantId == null 
                                       && x.Source == source 
                                       && x.ExternalStatusKey == externalKey);

            if (globalMap != null) return globalMap.InternalStatusId;

            // 3. Статус неизвестен системе!
            // Регистрируем его, чтобы пользователь увидел в админке и мог настроить
            await RegisterUnknownStatus(tenantId, source, externalKey);
            
            // Возвращаем статус "Требует внимания"
            return (int)SystemStatus.MappingError; 
        });
    }

    private async Task RegisterUnknownStatus(Guid tenantId, MarketplaceType source, string key)
    {
        // Создаем запись с пустым InternalStatusId, чтобы вывести её в UI как "Не настроено"
        var mapping = new StatusMapping
        {
            TenantId = tenantId,
            Source = source,
            ExternalStatusKey = key,
            ExternalStatusName = key, // Пока так
            InternalStatusId = (int)SystemStatus.MappingError // Временная заглушка
        };
        _db.StatusMappings.Add(mapping);
        await _db.SaveChangesAsync();
    }
}
```

---

### 3. Интеграция в "Кастер" (Маппер)

В твоем `OzonMapper` (из обсуждения про импорт) ты внедряешь `StatusResolver`.

```csharp
public class OzonMapper : IOzonMapper
{
    private readonly StatusResolver _statusResolver;
    private readonly Guid _currentTenantId;

    public async Task<Order> MapAsync(OzonPostingDto dto)
    {
        var order = new Order();
        // ... маппинг полей ...

        // РЕЗОЛВИНГ СТАТУСА
        // Ozon присылает статус в поле `status` (например "awaiting_packaging")
        order.StatusId = await _statusResolver.ResolveAsync(
            _currentTenantId, 
            MarketplaceType.Ozon, 
            dto.status
        );

        return order;
    }
}
```

---

### 4. UI: Управление Маппингом (User Control)

Как дать пользователю управлять этим?

**Экран "Настройка статусов интеграций":**

Это таблица, сгруппированная по Маркетплейсам.

| Маркетплейс | Статус Маркетплейса (Внешний) | Статус в системе (Внутренний) | Действие |
| :--- | :--- | :--- | :--- |
| **Ozon** | Ожидает упаковки (`awaiting_packaging`) | `[ Новый ]` (Dropdown) | 💾 |
| **Ozon** | Доставляется (`delivering`) | `[ Отгружен ]` (Dropdown) | 💾 |
| **WB** | Продано (`sold`) | `[ Выполнен ]` (Dropdown) | 💾 |
| **WB** | **Неизвестно (`refused_by_client`)** | `[ ⚠️ Выберите статус ]` | 💾 |

**Функционал:**
1.  **Авто-обнаружение:** Когда система встречает новый статус при импорте (через `RegisterUnknownStatus`), он появляется в этой таблице с пометкой "Не настроено". Пользователь заходит и видит: "Ого, WB добавил новый статус, надо привязать его к 'Отмене'".
2.  **Сброс к дефолту:** Кнопка "Вернуть настройки по умолчанию" (удаляет запись из `StatusMappings` с TenantId, и начинает работать глобальная настройка).
3.  **Множественный выбор:** Пользователь может слинковать 5 разных статусов Ozon в 1 статус системы "В работе".

---

### 5. Обратная синхронизация (Reverse Sync) 🔄

Сложнее, когда мы меняем статус в системе, и он должен улететь в Маркетплейс.
Здесь **нет однозначности** (One-to-Many).
Например: Внутренний `Отмена` -> В Ozon это может быть `cancelled-by-seller` или `cancelled-by-customer`.

**Решение:**
В "Конструкторе Транзакций" (который мы обсуждали ранее), при настройке перехода в статус "Отмена", система должна спросить:
*"При переходе в Отмену, какой статус отправить в Ozon?"*

Это настраивается уже не в общей карте, а в **Настройке Транзакции**:
```json
// TransactionRule для "Отмена заказа"
{
  "Actions": [
    {
      "Type": "OzonUpdateStatus",
      "Config": { "TargetStatus": "cancelled-out-of-stock" } // Пользователь выбрал конкретную причину
    }
  ]
}
```

### Итоговый алгоритм для Роадмапа:

1.  Создать справочник `StatusMappings` (БД).
2.  Заполнить `Seed Data` глобальными маппингами для популярных МП (чтобы клиент сразу мог работать).
3.  Реализовать `StatusResolver` с кэшированием.
4.  Встроить резолвер во все адаптеры импорта.
5.  Сделать UI страницу, где пользователь видит таблицу соответствия и может менять правый столбец (Внутренний статус).
6.  Реализовать "Ловушку": если пришел неизвестный статус, заказ создается со статусом "Ошибка маппинга", а админ получает алерт "Настройте новый статус".